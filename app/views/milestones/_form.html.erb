<%= form_with model: [ project, milestone ], class: "mt-6 space-y-4" do |form| %>
  <%= render "shared/form_errors", record: milestone, name: "milestone" %>

  <%= render layout: "shared/field", locals: { form: form, attribute: :title } do |field| %>
    <%= form.text_field :title, **field %>
  <% end %>

  <%= render layout: "shared/field", locals: { form: form, attribute: :description } do |field| %>
    <%= form.text_area :description, rows: 4, **field %>
  <% end %>

  <%= render layout: "shared/field", locals: { form: form, attribute: :due_on, label: "Due on" } do |field| %>
    <%= form.date_field :due_on, **field %>
  <% end %>

  <%= render layout: "shared/field", locals: { form: form, attribute: :state } do |field| %>
    <%= form.select :state, Milestone::STATES.map { |state| [ state.titleize, state ] }, {}, field %>
  <% end %>

  <div class="flex items-center gap-4">
    <%= form.submit class: "cursor-pointer rounded-md bg-slate-900 px-3 py-2 text-sm font-medium text-white transition-colors hover:bg-slate-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-slate-900" %>
    <%# Cancel, not a back link: one way out, and it lands where the form was opened from. %>
    <%= link_to "Cancel", cancel_path, class: "text-sm font-medium text-slate-600 transition-colors hover:text-slate-900 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2" %>
  </div>
<% end %>
