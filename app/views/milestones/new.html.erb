<% content_for :title, "New milestone" %>

<div class="mx-auto max-w-3xl px-4 py-8">
  <h1 class="text-2xl font-semibold text-slate-900">New milestone</h1>
  <%= render "form", project: @project, milestone: @milestone, cancel_path: project_milestones_path(@project) %>
</div>
