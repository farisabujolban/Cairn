<% content_for :title, "Edit #{@milestone.title}" %>

<div class="mx-auto max-w-3xl px-4 py-8">
  <h1 class="text-2xl font-semibold text-slate-900">Edit milestone</h1>
  <%= render "form", project: @project, milestone: @milestone %>
</div>
