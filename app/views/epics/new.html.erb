<% content_for :title, "New epic" %>

<div class="mx-auto max-w-3xl px-4 py-8">
  <h1 class="text-2xl font-semibold text-slate-900">New epic</h1>
  <%= render "form", project: @project, epic: @epic, cancel_path: project_epics_path(@project) %>
</div>
