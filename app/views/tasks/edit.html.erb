<% content_for :title, "Edit #{@task.title}" %>

<div class="mx-auto max-w-3xl px-4 py-8">
  <h1 class="text-2xl font-semibold text-slate-900">Edit task</h1>
  <%= render "form", project: @project, task: @task,
        form_url: project_task_path(@project, @task),
        cancel_path: project_task_path(@project, @task) %>
</div>
